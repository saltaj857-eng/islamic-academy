 "use client"; import {useState} from "react"; import {createBrowserClient} from "@supabase/ssr";
export default function Auth(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [msg,setMsg]=useState("");async function go(e:React.FormEvent){e.preventDefault();const s=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!);const r=await s.auth.signInWithPassword({email,password});setMsg(r.error?.message||"Вход выполнен");}return <main className="wrap section"><div className="card" style={{maxWidth:480,margin:"auto"}}><h1>Вход</h1><form onSubmit={go}><input className="input" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" placeholder="Пароль" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn">Войти</button></form><p>{msg}</p></div></main>}